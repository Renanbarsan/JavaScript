/*let num = 0.0, soma = 0.0, media, total = 0.0;
 
while(num >= 0){
    num = prompt("Insira um número: ")
    
    if(num >= 0){
        soma = parseInt(soma + parseInt(num))
        total++
    }
}

media = soma / total;

alert("Soma de todos os números digitados: " + soma);
alert("Média de todos os números digitados: " + media);
alert("Todos os números digitados: " + total);
*/

let num = 0.0;
    num = prompt("Digite um número: ");

    while(num <= 100){
        num = num * 3
        alert(num);
    }
